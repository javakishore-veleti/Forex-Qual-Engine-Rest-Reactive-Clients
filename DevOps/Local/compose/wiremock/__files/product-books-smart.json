{
  "requestedBookIds": {{jsonPath request.body '$.bookIds'}},
"validBookIds": {{jsonPath request.body '$.bookIds'}},
"invalidBookIds": [],
"bookDetails": [
{{#each (jsonPath request.body '$.bookIds')}}
{
"bookId": "{{this}}",
"productType": "FX-SPOT",
"riskCategory": "LOW",
"limits": { "min": 10000, "max": 5000000 }
}{{#unless @last}},{{/unless}}
{{/each}}
]
}
